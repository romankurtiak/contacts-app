import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: { flex: 1 },
  header: {
    padding: 20,
    gap: 10,
  },
  headerTitle: {
    fontSize: 16,
    textAlign: 'center',
    color: '#000',
  },
  body: {
    paddingHorizontal: 20,
    gap: 7,
  },
});
